# Operator identity (not a secret). Only this email can grant/revoke app access
# and list every user / every board. Alias: DATABSED_SYSTEM_EMAIL
SYSTEM_USER_EMAIL=

# Persistence (Vercel)
# KV_REST_API_URL=
# KV_REST_API_TOKEN=
# or UPSTASH_REDIS_REST_URL / UPSTASH_REDIS_REST_TOKEN
# or BLOB_READ_WRITE_TOKEN
# STORE_BACKEND=kv|blob|fs
